// One way to enforce separation of concernsis by using the Container/Presentaional Pattern
// with this patterns, we separate the view from the application logic.
// In many cases, the Container/Presentaional pattern replaced with React Hooks.
// Hooks make it easy to separate logic and view in component, just like the Container/Presentaional Pattern.
